name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm test
      - name: Example security report
        run: node dist/cli.js run --config mcp-preflight.config.json --fail-on none
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mcp-preflight-report
          path: reports/
